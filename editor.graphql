{
    red: hexColor(color: {name: RED, mode: DARK}) {
        ...colorFields
    }
    green: hexColor(color: {name: GREEN, mode: LIGHT}) {
        ...colorFields
    }
}

fragment colorFields on ColorType {
    name
    mode
}