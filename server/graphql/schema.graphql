type Book
{
    id: ID!
    title: String!
    genre: String!
    publish_at: String!
}

type Author
{
    id: ID!
    title: String!
    age: Int!
}

# Special Type
type Query {
    authors: [Author!]!
    author(id: ID!=1): Author
    books: [Book!]!
    book(id:ID!): Book
}